import json from '../../../../../fixtures/report.json'

export default function ReportPage({ params }: { params: { slug: string } }) {
    const {data} = json
    return (
      <>
      </>
    )
  }